Dockerfile with Racket language (http://racket-lang.org), based on Debian Sid
---
Examples:

* raco:
```
docker run --rm -ti loadaverage/racket raco make ...
```
* REPL:
```
docker run --rm -ti loadaverage/racket
```
